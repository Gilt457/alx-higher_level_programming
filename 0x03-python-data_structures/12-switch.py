#!/usr/bin/python3
# Swap the values of two variables
a = 89
b = 10
temp = a  # Store the value of a in a temporary variable
a = b  # Assign the value of b to a
b = temp  # Assign the value of temp to b
print(f"a={a} - b={b}")  # Use f-strings to format the output
